# Copy this to .env and fill in ONE of these — the engine tries OpenAI first, Gemini second
# If neither is set, the demo fallback response is used (safe for pitch day)

OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...
