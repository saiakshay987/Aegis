export default function Card({ children, className = '', hover = false, glow = false }) { return <div className={`glass rounded-2xl ${hover ? 'card-hover' : ''} ${glow ? 'shadow-lg' : ''} ${className}`}>{children}</div> }
export function CardHeader({ title, subtitle, icon, action }) { return <div className="flex items-start justify-between mb-4"><div className="flex items-center gap-3">{icon&&<div className="icon-tile !w-8 !h-8">{icon}</div>}<div><h3 className="font-semibold text-slate-900">{title}</h3>{subtitle&&<p className="text-xs text-slate-500 mt-0.5">{subtitle}</p>}</div></div>{action&&<div>{action}</div>}</div> }
